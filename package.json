{
  "name": "tabassa",
  "private": true,
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run --parallel build",
    "pretify": "prettier --write .",
    "docs:dev": "yarn workspace docs dev"
  },
  "devDependencies": {
    "@types/node": "^17.0.30",
    "lerna": "^4.0.0",
    "typescript": "^4.6.4"
  },
  "workspaces": [
    "packages/*",
    "docs"
  ]
}
